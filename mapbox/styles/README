These are mapbox styles created in Mapbox studio and retrieved using the Mapbox API. They can be used to recreate the style on new accounts if needed, but will need to be modified to remove created and modified properties etc. For more information see https://docs.mapbox.com/api/maps
